extend ../../../views/layout

block content
  
  .row
    form(action="/user/confirm/" + hash, method="POST").small-12.columns
      fieldset
        legend Further details

        .row
          .small-6.columns.small-offset-3

            label Login:
            input(type="text", name="login")

            label Password:
            input(type="password", name="password")

            label Password (confirm):
            input(type="password", name="password_confirm")

            input(type="submit", value="Register").button.radius